{
  "name": "nx-gradle-plus",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "nx-gradle-plus/src",
  "projectType": "library",
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/nx-gradle-plus",
        "main": "nx-gradle-plus/src/index.ts",
        "tsConfig": "nx-gradle-plus/tsconfig.lib.json",
        "assets": [
          "nx-gradle-plus/*.md",
          {
            "input": "./nx-gradle-plus/src",
            "glob": "**/!(*.ts)",
            "output": "./src"
          },
          {
            "input": "./nx-gradle-plus/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          },
          {
            "input": "./nx-gradle-plus",
            "glob": "generators.json",
            "output": "."
          },
          {
            "input": "./nx-gradle-plus",
            "glob": "executors.json",
            "output": "."
          }
        ]
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "nx-gradle-plus/jest.config.ts"
      }
    }
  }
}
